set(
	SOURCE_FILES
    "${CMAKE_CURRENT_SOURCE_DIR}/utility.c"
	"${CMAKE_CURRENT_SOURCE_DIR}/utf8.c"
)
set(RELATIVE_PATH "src/utf8")

if(SOURCE_FILES)
    target_sources("${PROJECT_NAME}" PRIVATE ${SOURCE_FILES})

    install(FILES ${SOURCE_FILES} DESTINATION ${RELATIVE_PATH})
endif()
